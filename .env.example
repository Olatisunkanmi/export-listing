DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=pgUser
DB_PASSWORD=pgPass1
DB_DATABASE=exportListing

DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}"
TEST_DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}_test"
PORT=3000
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
AUTH_THROTTLE_LIMIT=30
AUTH_THROTTLE_TTL_MS=60000
